node_modules/
.venv/
dist/
.env
.env.local
.env.*
!.env.example
!.env.template
.DS_Store
*.tsbuildinfo
playwright-report/
test-results/

# Per-project Databricks auth file written by the Demo Prompt Generator
# at runtime (deployed mode). Contains a user PAT — never commit or
# ship with a template snapshot. See backend/AUTH.md in the generator.
.databrickscfg
