# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv

# version file
src/demo_prompt_generator/_version.py

# metadata file
src/demo_prompt_generator/_metadata.py

# Runtime data staged INTO the package by scripts/build.sh before `uv build`
# (cleaned up by the build's EXIT trap; gitignored as a safety net).
src/demo_prompt_generator/.claude/
src/demo_prompt_generator/initial_templates/
src/demo_prompt_generator/ai_dev_kit/

# build files
dist

# js-related
node_modules/
.tanstack

# env files
.env
.env.local
.env.*
!.env.example

# mac
.DS_Store

# DABs
.databricks
.build/

# per-target deploy configs (e.g. databricks.prod.yml). Examples are committed.
databricks.*.yml
!databricks.*.yml.example

# Legacy dev tooling
.apx/

# PGLite local database
.pglite/

# UI dist folder (built frontend assets)
**/__dist__/

# Project local storage
projects/

# Cloned ai-dev-kit repository
ai_dev_kit/

# Electron build artifacts
dist-electron/
dist-backend/
dist-python/
dist-venv/
build-pyinstaller/
run_server.py
build-resources/*.icns
*.dmg
electron/config.json
